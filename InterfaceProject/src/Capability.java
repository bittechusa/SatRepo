
public interface Capability extends WebDriver,RemoteDriver//a interface can extends more than one interface
{
	//When u extends more than one interface means all unimplemented method already in this interface like class

}
